{
  "name": "clara-stt-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone WebSocket relay: browser audio -> Deepgram live API. Deployed separately from the main Clara app because it needs a persistent process (Vercel serverless can't hold WebSockets open).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
